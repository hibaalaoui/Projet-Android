<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/report_content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- 1. Résumé chiffré amélioré -->
        <TextView
            android:id="@+id/tvReportSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Résumé du rapport"
            android:textSize="18sp"
            android:paddingBottom="16dp" />

        <!-- 2. CombinedChart : barres (revenus) + ligne (dépenses) -->
        <com.github.mikephil.charting.charts.CombinedChart
            android:id="@+id/chartCombined"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginBottom="24dp" />

        <!-- 3. LineChart : tendance d’épargne cumulative -->
        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/chartTrend"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginBottom="24dp" />

        <!-- 4. Doughnut (PieChart avec trou) : répartition par catégories de dépenses -->
        <com.github.mikephil.charting.charts.PieChart
            android:id="@+id/chartCategories"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginBottom="24dp" />

        <com.github.mikephil.charting.charts.LineChart
            android:id="@+id/chartMonthlyTrend"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:layout_marginTop="16dp"/>

        <!-- 5. Bouton exporter -->
        <Button
            android:id="@+id/btnExportPdf"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Exporter en PDF" />

    </LinearLayout>
</ScrollView>
